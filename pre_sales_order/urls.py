from django.urls import path
from .views import *

urlpatterns = [
    # path('sales-order/', SalesOrderList.as_view(), name='sales-order'),
    # path('sales-order/<str:pk>', SalesOrderDetails.as_view(), name='sales-order'),
    # path('update-status-sales/<str:pk>/<str:return>', StatusUpdateView.as_view(), name='update-status-sales'),
    # path('create-so/', create_so, name='create-so'),
    # path('preview-so/<str:pk>', so_view.as_view(), name='preview-so'),
    # path('delete-sales-order/<str:pk>', SalesOrderDeleteView.as_view(), name="delete-sales-order"),

    # # documents
    # path('sales-order-doc/', SalesOrderDocTemplate.as_view(), name="sales-order-doc"),
    
]